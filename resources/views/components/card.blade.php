<div class="card">
    <div class="card-header">
        @yield('card-header')
    </div>
    <div class="card-body">
        <h5 class="card-title">@yield('card-title')</h5>
        <p class="card-text">@yield('card-text')</p>
        <a href="#" class="btn btn-primary">@yield('card-button')</a>
    </div>
</div>
